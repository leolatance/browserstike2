/** XP formulas live in the engine now (the edge function uses them too). */
export { XP, attrCap, xpForLevel, performanceMult, minigameMult, matchXp, applyXp, clampNum as clamp } from '@idle-strike/engine';
export type { MatchXpInput, MatchXpBreakdown, LevelState } from '@idle-strike/engine';
