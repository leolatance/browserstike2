import { ENGINE_VERSION } from '@idle-strike/engine';

// Placeholder scaffold. Screens arrive in a later step (see CLAUDE.md roadmap).
export function App() {
  return (
    <main>
      <h1>Idle Strike 2</h1>
      <p>engine v{ENGINE_VERSION}</p>
    </main>
  );
}
